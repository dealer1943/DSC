# Adapters

| Module | Status |
|--------|--------|
| `flywire_pack.py` | v0 — load / focus / status / signals |
| `dsc_runtime.py` | scaffold — `MODEL/active/manifest.json` only |
| `male_cns.py` / `hemibrain.py` | stubs |

Contract: see `UI/README.md` and `adapters/base.py`.
